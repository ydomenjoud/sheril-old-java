<?php

$file_path = __DIR__ . '/../game.txt';
$game_name = "dune";
if (file_exists($file_path)) {
    include_once $file_path;
}

function base_flotte($base, $commandant)
{
    $tab = [];
    $result = mysql_query("SELECT * FROM diviser_flotte WHERE NUMERO='$commandant'");
    $nb_lignes = mysql_num_rows($result);
    for ($i = 0; $i < $nb_lignes; $i++) {
        $rf = mysql_fetch_row($result);
        $num = $rf[1] + 1;
        $tab[10000 + $rf[3]] = "$rf[2] (divisée à partir de la flotte $num)";
    }
    return $tab;
}

function base3($base, $commandant, $b)
{
    $result = mysql_query("SELECT * FROM $b WHERE NUMERO='$commandant'");
    if (!$result) {
        echo mysql_error();
    }
    return r_base2($result, 0);
}

function base4($base, $b)
{
    $result = mysql_query("SELECT * FROM $b");
    if (!$result) {
        echo mysql_error();
    }
    return r_base2($result, 1);
}

function base1($base, $commandant, $b)
{
    $result = mysql_query("SELECT * FROM $b WHERE NUMERO='$commandant'");
    return r_base($result, 0);
}

function base2($base, $b)
{
    $result = mysql_query("SELECT * FROM $b");
    return r_base($result, 1);
}

function r_base2($result, $mod)
{
    $nb_lignes = mysql_num_rows($result);
    if(!$nb_lignes) {
        echo mysql_error();
    }
    if ($nb_lignes == 0) return "";
    $rf = mysql_fetch_row($result);
    return explode(",", trim(ereg_replace("#", "'", $rf[1 - $mod])));
}

function r_base($result, $mod)
{
    $nb_lignes = mysql_num_rows($result);
    if ($nb_lignes == 0) return "";
    $rf = mysql_fetch_row($result);
    $tra = explode(",", trim(ereg_replace("#", "'", $rf[1 - $mod])));
    $tra2 = explode(",", trim(ereg_replace("#", "'", $rf[2 - $mod])));
    for ($i = 0; $i < sizeof($tra); $i++)
        $t[$tra[$i]] = $tra2[$i];
    return $t;
}


//function array_keys($t){
// if($t=="") return "";
// $i=0;
// for(reset($t); $k = each($t); ) {
//  $r[$i]=$k[0];
//  $i++;
//  }
// return $r;
// }

function fusion_tab($tab1, $tab2)
{
    $j = sizeof($tab1);
    for ($i = 0; $i < sizeof($tab2); $i++)
        $tab1[$j + $i] = $tab2[$i];
    return $tab1;
}

function fusion_tab2($tab1, $tab2)
{
    $j = sizeof($tab1);
    $k = array_keys($tab2);
    for ($i = 0; $i < sizeof($k); $i++) {
        $n = $k[$i];
        $tab1[$n] = $tab2[$n];
    }
    return $tab1;
}

function V($name, $default="") {
    return array_key_exists($name,$_POST) ? $_POST[$name] : (array_key_exists($name,$_GET) ? $_GET[$name] : $default);
}

function select1($nv, $t, $class='select', $multiple=false, $maxWidth = '200')
{

    echo("<SELECT SIZE=\"".($multiple ? 10 : 1)."\" NAME=\"$nv".($multiple ? '[]' : '')."\" style='max-width: {$maxWidth}px; display: inline-flex;' class='$class' ".($multiple ? 'multiple' : ''). ">");
    $k = array_keys($t);
    // Récupération de la valeur postée une seule fois pour plus de clarté
    $postedValue = isset($_POST[$nv]) ? $_POST[$nv] : null;
    for ($i = 0; $i < sizeof($k); $i++) {
        $j = $k[$i];
        $selected = '';

        if ($multiple) {
            // Cas multiple : on vérifie si la clé appartient au tableau
            if (is_array($postedValue) && in_array($j, $postedValue)) $selected = 'selected ';
        } else {
            // Cas simple : comparaison classique
            if ($postedValue == $j) $selected = 'selected ';
        }

        echo("<OPTION ".$selected." ");
        echo("VALUE=\"$j\">$t[$j]");
    }
    echo("</SELECT>");
}

function select3($nv, $t)
{
    echo("<SELECT SIZE=\"1\" NAME=\"$nv\">");
    for ($i = 0; $i < sizeof($t); $i++) {
        echo("<OPTION ".((array_key_exists($nv,$_POST) && $_POST[$nv] == $t[$i]) ? 'selected ' : ''));
        echo("VALUE=\"$t[$i]\">$t[$i]");
    }
    echo("</SELECT>");
}

function select_nb($nv, $a, $b, $selectValue=null)
{
    echo("<SELECT NAME=\"$nv\">");
    for ($i = $a; $i < $b; $i++) {
        echo("<OPTION ".(((array_key_exists($nv,$_POST) && $_POST[$nv] == $i) || ($selectValue !== null && $selectValue === $i)) ? 'selected ' : ''));
        echo("VALUE=\"$i\">$i");
    }
    echo("</SELECT>");
}

function select_pos($nv)
{
     echo "<input name='$nv' min=0 max=40 required style='width: 50px;' type='number' value='".(array_key_exists($nv,$_POST) ? $_POST[$nv] : '')."' maxlength=2 />";
//    select_nb($nv, 1, 41);
}

function select_pour_cent($nv)
{
    select_nb($nv, 0, 101);
}

function select_planete($nv)
{
    echo("<SELECT NAME=\"$nv\">");
    for ($i = 1; $i < 21; $i++) {
        $inter = $i - 1;
        echo("<OPTION VALUE=\"$inter\" ".((array_key_exists($nv,$_POST) && $_POST[$nv] == $inter) ? 'selected ' : '').">$i");
    }
    echo("</SELECT>");
}

function select_planete2($nv)
{
    echo("<SELECT NAME=\"$nv\">");
    echo("<OPTION SELECTED VALUE=\"100\">");
    for ($i = 1; $i < 21; $i++) {
        $inter = $i - 1;
        echo("<OPTION VALUE=\"$inter\">$i");
    }
    echo("</SELECT>");
}

function display_planete($nb, $title = "planète" ){
    if($nb!=100) {
        return " ($title <SPAN class=\"item\">" . ( $nb + 1 ). "</SPAN> ) ";
   }
}

function POST($key) {
    return array_key_exists($key,$_POST) ? $_POST[$key] : '';
}

function baseList($table, $id=null, $where=null)
{
    $query = "SELECT * FROM $table";
    if($where) {
        $query .= " WHERE $where";
    }
    $resultList = mysql_query($query);
    if (!$resultList) {
        echo mysql_error();
    }
    $list = [];
    while ($row = mysql_fetch_assoc($resultList)) {
        if($id){
            $list[$row[$id]] = $row;
        }
        else $list[] = $row;
    }
    return $list;
}

/**
 * Retourne l'index de la colonne "id" dans une table donnée.
 * * @param string $nom_table Le nom de la table à vérifier
 * @param resource $connexion (Optionnel) La ressource de connexion MySQL
 * @return int L'index (0-based) ou -1 si non trouvé
 */
function get_id_column_index($nom_table) {
    // 1. Exécuter une requête légère pour obtenir la structure (LIMIT 0 suffit)
    $sql = "SELECT * FROM " . mysql_real_escape_string($nom_table) . " LIMIT 0";
    $result = mysql_query($sql);

    if (!$result) {
        return -1; // La table n'existe probablement pas ou erreur SQL
    }

    // 2. Récupérer le nombre total de colonnes
    $nb_champs = mysql_num_fields($result);

    // 3. Parcourir les colonnes pour trouver celle nommée "id"
    for ($i = 0; $i < $nb_champs; $i++) {
        $nom_colonne = mysql_field_name($result, $i);

        // Comparaison insensible à la casse (optionnel, selon vos besoins)
        if (strtolower($nom_colonne) === 'id') {
            return $i; // On a trouvé l'index !
        }
    }

    // 4. Si on sort de la boucle sans avoir trouvé
    return -1;
}

function getCommandantHTML($num) {
    if (empty($num)) return "Inconnu";
    global $base;
    $num = (int)$num;
    $sql = "SELECT NOM, NUMERO, RACE FROM aa_registre WHERE NUMERO = $num";
    $res = @mysql($base, $sql);
    if ($res && $row = @mysql_fetch_assoc($res)) {
        return "<span class='race{$row['RACE']}'>{$row['NOM']} ({$row['NUMERO']})</span>";
    }
    return "Inconnu (" . $num . ")";
}
function display_author($name, $numero, $race){
    return "<span class='race$race'>$name ($numero)</span>";
}

function estDansUnPartage($commandant){
    $result = mysql_query("SELECT * FROM _share_technology WHERE NUMERO = $commandant");
    return mysql_num_rows($result)>0;
}
function getTechnoCodeReceived($commandant){
    $result = mysql_query("SELECT TECHNOLOGIE FROM transferer_technologie WHERE BENEFICIAIRE = $commandant");
    if(mysql_num_rows($result)>0){
        $row = mysql_fetch_array($result);
        return $row['TECHNOLOGIE'];
    }
    return null;
}

function slugify(string $text) {
    // 1. Remplace les caractères non-ASCII (accents, etc.) par leur équivalent ASCII
    $text = transliterator_transliterate('Any-Latin; Latin-ASCII; Any-Lower;', $text);

    // 2. Supprime tout ce qui n'est pas une lettre, un chiffre ou un espace
    $text = preg_replace('/[^a-z0-9\s-]/', '', $text);

    // 3. Remplace les espaces et tirets consécutifs par un seul tiret
    $text = preg_replace('/[\s-]+/', '-', $text);

    // 4. Supprime les tirets au début et à la fin
    return trim($text, '-');
}

function filterList($table, $field, $listToFilter){
    global $base, $commandant;
    $field = mysql_real_escape_string($field);
    $table = mysql_real_escape_string($table);
    $commandant = intval($commandant);

    $result = mysql($base, "SELECT $field FROM $table WHERE NUMERO=$commandant");
    $alreadyUsedKeys = array();
    while ($row = mysql_fetch_assoc($result)) {
        $alreadyUsedKeys[] = $row[$field];
    }
    return array_diff_key($listToFilter, array_flip($alreadyUsedKeys));
}

function regrouperFlottes(array $ordres) {
    $graph = [];

    // 1. Construction du graphe (non orienté)
    foreach ($ordres as $ordre) {
        $f1 = $ordre[0];
        $f2 = $ordre[1];

        $graph[$f1][] = $f2;
        $graph[$f2][] = $f1;
    }

    $visites = [];
    $groupes = [];

    // 2. Parcours pour trouver chaque composante connexe
    foreach (array_keys($graph) as $noeud) {
        if (!isset($visites[$noeud])) {
            $groupeActuel = [];

            // Exploration en profondeur (DFS) via une pile
            $pile = [$noeud];
            $visites[$noeud] = true;

            while (!empty($pile)) {
                $courant = array_pop($pile);
                $groupeActuel[] = $courant;

                foreach ($graph[$courant] as $voisin) {
                    if (!isset($visites[$voisin])) {
                        $visites[$voisin] = true;
                        $pile[] = $voisin;
                    }
                }
            }

            // Trier les numéros de flotte dans le groupe
            sort($groupeActuel);

            // Formatage sous forme de chaîne "0, 8, 9, 10..." ou de sous-tableau
            $groupes[] = $groupeActuel;
        }
    }

    return $groupes;
}